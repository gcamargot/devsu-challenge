# Run `pre-commit install` once after cloning to enable these hooks.
# TODO: agregar terraform_fmt/validate cuando se sumen los .tf
repos:
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v5.0.0
    hooks:
      - id: trailing-whitespace
      - id: end-of-file-fixer
      - id: check-yaml
        # --unsafe: mkdocs.yml usa tags YAML propios de Python (!!python/name) para mermaid
        args: [--allow-multiple-documents, --unsafe]
      - id: check-json
      - id: check-merge-conflict
      - id: check-added-large-files
        args: [--maxkb=1024]
      - id: mixed-line-ending

  - repo: local
    hooks:
      - id: eslint
        name: eslint (app)
        entry: bash -c 'cd app && npm run lint'
        language: system
        files: \.js$
        exclude: ^app/(node_modules|coverage)/
        pass_filenames: false
