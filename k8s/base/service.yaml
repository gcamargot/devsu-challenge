apiVersion: v1
kind: Service
metadata:
  name: devsu-demo
  labels:
    app.kubernetes.io/name: devsu-demo
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: devsu-demo
  ports:
    - name: http
      port: 80
      targetPort: http
