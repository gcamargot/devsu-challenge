apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: devsu

resources:
  - namespace.yaml
  - serviceaccount.yaml
  - configmap.yaml
  - deployment.yaml
  - service.yaml
  - hpa.yaml
  - pdb.yaml
  - ingress.yaml
  - networkpolicy.yaml
# secret.example.yaml is a template only; the real Secret comes from the overlay
