# Plantilla. El Secret real se crea fuera de git (kubectl, secretGenerator del overlay,
# o inyectado por el CD desde Azure). No commitear credenciales reales.
apiVersion: v1
kind: Secret
metadata:
  name: devsu-demo-secret
type: Opaque
stringData:
  DB_USER: "devsu"
  DB_PASSWORD: "CHANGEME"
