# Local clusters can't be reached by Let's Encrypt, so issue a self-signed cert
# here just to exercise the cert-manager + ingress TLS wiring. AKS uses real LE.
apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: selfsigned
spec:
  selfSigned: {}
