apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: devsu

# Extends the local-kind overlay: 3 replicas + ingress cookie affinity.
resources:
  - ../local-kind

patches:
  - path: patch-deployment.yaml
  - path: patch-ingress-affinity.yaml
